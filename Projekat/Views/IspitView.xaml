<UserControl x:Class="Projekat.Views.IspitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#5DADE2"/>
        <SolidColorBrush x:Key="LightBlue" Color="#D6EAF8"/>
        <SolidColorBrush x:Key="ButtonBlue" Color="#85C1E9"/>
        <SolidColorBrush x:Key="DarkBlueText" Color="#1B4F72"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="{StaticResource DarkBlueText}"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="TextBox" x:Key="ValidatedTextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="{StaticResource ButtonBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,10,10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource LightBlue}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Evidencija ispita"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,15"
                   Foreground="{StaticResource DarkBlueText}"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Ispiti}"
                  SelectedItem="{Binding SelectedIspit}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Background="White"
                  FontSize="15"
                  RowBackground="#EBF5FB"
                  AlternatingRowBackground="#D6EAF8"
                  Margin="0,0,0,15"
                  ColumnWidth="*">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Student" Binding="{Binding Student.FullDisplay}" />
                <DataGridTextColumn Header="Predmet" Binding="{Binding Predmet.Naziv}" />
                <DataGridTextColumn Header="Ocena" Binding="{Binding Ocena}" />
                <DataGridTextColumn Header="Datum" Binding="{Binding DatumPolaganja, StringFormat=d}" />
                <DataGridTextColumn Header="Ispitni rok" Binding="{Binding IspitniRok}" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Student:" Grid.Row="0" Grid.Column="0"/>
            <ComboBox Grid.Row="0" Grid.Column="1"
                      ItemsSource="{Binding Students}"
                      SelectedItem="{Binding SelectedStudent}"
                      DisplayMemberPath="FullDisplay"/>

            <TextBlock Text="Predmet:" Grid.Row="1" Grid.Column="0"/>
            <ComboBox Grid.Row="1" Grid.Column="1"
                      ItemsSource="{Binding Predmets}"
                      SelectedItem="{Binding SelectedPredmet}"
                      DisplayMemberPath="Naziv"/>

            <TextBlock Text="Ocena:" Grid.Row="2" Grid.Column="0"/>
            <TextBox Grid.Row="2" Grid.Column="1"
                     Text="{Binding Ocena, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                     Style="{StaticResource ValidatedTextBox}"/>

            <TextBlock Text="Datum polaganja:" Grid.Row="3" Grid.Column="0"/>
            <DatePicker Grid.Row="3" Grid.Column="1"
                        SelectedDate="{Binding DatumPolaganja}"/>

            <TextBlock Text="Ispitni rok:" Grid.Row="4" Grid.Column="0"/>
            <ComboBox Grid.Row="4" Grid.Column="1"
                      ItemsSource="{Binding IspitniRokoviLista}"
                      SelectedItem="{Binding IspitniRok}"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add" Command="{Binding AddCommand}" />
            <Button Content="Edit" Command="{Binding EditCommand}" />
            <Button Content="Delete" Command="{Binding DeleteCommand}" />
        </StackPanel>

    </Grid>
</UserControl>